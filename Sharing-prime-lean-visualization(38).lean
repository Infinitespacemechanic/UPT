def band_yellow : Float := 1.5 -- Earth high density Measured c
def band_green : Float := 0.044 -- outer reaches free dust Faster LowDensity
def band_purple : Float := 1.0 -- n=5 composite Base-60 Bridge
def always_something (h : Float) : Bool := h > 0 -- Always Something Never Nothing
def above_below_same : Bool := true -- bingo top pink = bottom light blue same field mobius